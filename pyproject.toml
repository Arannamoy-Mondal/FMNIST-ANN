[project]
name = "fmnist-ann"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arannamoy Mondal", email = "arannamoymondal13@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "torchvision",
    "torch",
    "pandas",
    "matplotlib"
]

[project.scripts]
fmnist-ann = "fmnist_ann:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
